import{Bot}from"grammy";const token=process.env.BOT_TOKEN;if(!token){console.error("BOT_TOKEN is not set; bot not started");process.exit(0)}const bot=new Bot(token);bot.command("start",ctx=>ctx.reply("Welcome to VAMIOS Bingo! Open the web app from your Telegram menu."));bot.command("help",ctx=>ctx.reply("Choose a stake, board, join a game, and play. This build uses demo credits only."));bot.start();
